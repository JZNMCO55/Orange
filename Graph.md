# 通用曲线渲染管线
## 背景
需要自定义一个与平台无关的曲线渲染管线，可以渲染各种类型的曲线，如折线、曲线、散点图、柱状图、饼图等。提供一个胶水接口层，根据需要去自主选择不同的图形API(OpenGL,Vulkan)或特定平台渲染接口(vtk,ybaf,filament)等，作为后端实现这个基础接口。

## 目标
1. 提供丰富的交互式曲线绘制功能，包括折线、曲线、散点图、柱状图、饼图等。
2. 提供丰富的交互体验
3. 提供丰富的数据分析功能，包括数据统计、数据分析、数据可视化等。
4. 提高曲线图的渲染性能，创建渲染友好的数据结构

## 基础类型
- GraphManager 图表管理器 (负责管理图表的创建、销毁、更新等)
- GraphData 图表数据 (负责管理图表的数据，包括数据源、数据类型、数据格式等)
- GraphProperty 图表属性 (负责管理图表的属性，包括颜色、线型、点型)
- GraphAxis 图表轴 (负责管理图表的轴，包括X轴、Y轴等)
- GraphLegend 图例 (负责管理图表的图例，包括图例的显示、隐藏等,可进行交互)
- GraphTooltip 图表提示 (负责管理图表的提示，包括提示的显示、隐藏等，鼠标悬浮时显示)
- GraphAnnotation 图表注释 (负责管理图表的注释，包括注释的显示、隐藏等，鼠标悬浮时显示)
- GraphRender 图表渲染器 (负责实现线段、文字等渲染细节，预分配内存，尽可能减少DrawCall次数)
- GraphSence 场景类(负责递交图表、图例、坐标轴、文字数据, 接收事件分发)
- Shader 着色器
- VertexArray 顶点数组
- FrameBuffer 帧缓冲区
- Camera 相机
- Buffer 缓冲区
- RenderCommand (渲染命令，主要作为胶水层)

## Todo
- 柱状图
- 饼状图
- 极坐标图
- 直角坐标图