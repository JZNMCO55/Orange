---
description: 生成代码选项
globs: 
alwaysApply: false
---
1. **头文件保护**  
   - 使用 `#ifndef` + `#define` + `#endif`  
   - 宏名称格式：`[FILENAME_UPPERCASE]_H_`  

2. **命名规范**  
   - 成员函数：`UpperCamelCase`（如 `GetName()`）  
   - 成员变量：  
     - 非指针 → `mValue`  
     - 指针 → `mpNode`  
   - 接口函数：必须添加 `/// @brief` 文档注释  

3. **禁止项**  
   - `#pragma once`  
   - 成员变量中的下划线（如 `m_`、`mp_`）
