# Orange引擎开发文档

## Memory系统技术文档

### 概述
Orange引擎的Memory系统是一个自定义内存管理框架，提供内存分配跟踪、统计分析和调试功能。系统包含自定义分配器、全局操作符重载、线程安全保护等企业级特性。

### Memory系统架构

#### 核心数据结构

**1. AllocationStats（分配统计）**
```cpp
struct AllocationStats
{
    size_t TotalAllocated = 0;  // 总分配字节数
    size_t TotalFreed = 0;      // 总释放字节数
};
```

**2. Allocation（分配记录）**
```cpp
struct Allocation
{
    void* Memory = 0;           // 分配的内存地址
    size_t Size = 0;            // 分配的内存大小（字节）
    const char* Category = 0;   // 分配类别标识（文件名或描述）
};
```

**3. Mallocator（自定义分配器模板）**
```cpp
template <class T>
struct Mallocator
{
    typedef T value_type;
    T* allocate(std::size_t n);
    void deallocate(T* p, std::size_t n) noexcept;
};
```

**4. AllocatorData（分配器数据存储）**
```cpp
struct AllocatorData
{
    std::map<const void*, Allocation> m_AllocationMap;  // 内存映射表
    AllocationStatsMap m_AllocationStatsMap;            // 统计映射表
    std::mutex m_Mutex, m_StatsMutex;                   // 线程同步锁
};
```

#### Allocator类接口

**初始化管理**
```cpp
// 初始化内存分配器
static void Init();

// 原始内存分配（不跟踪）
static void* AllocateRaw(size_t size);
```

**内存分配接口**
```cpp
// 基础分配（带跟踪）
static void* Allocate(size_t size);

// 带描述的分配（用于分类统计）
static void* Allocate(size_t size, const char* desc);

// 带文件行号的分配（用于调试）
static void* Allocate(size_t size, const char* file, int line);

// 内存释放
static void Free(void* memory);
```

**统计信息**
```cpp
// 获取分配统计信息
static const AllocationStatsMap& GetAllocationStats();

// 获取全局统计
const AllocationStats& Memory::GetAllocationStats();
```

### 全局操作符重载（Windows平台）

Memory系统在Windows平台下提供完整的全局new/delete操作符重载：

```cpp
// 基础操作符
void* operator new(size_t size);
void* operator new[](size_t size);
void operator delete(void* memory);
void operator delete[](void* memory);

// 带描述的操作符
void* operator new(size_t size, const char* desc);
void* operator new[](size_t size, const char* desc);
void operator delete(void* memory, const char* desc);
void operator delete[](void* memory, const char* desc);

// 带文件行号的操作符
void* operator new(size_t size, const char* file, int line);
void* operator new[](size_t size, const char* file, int line);
void operator delete(void* memory, const char* file, int line);
void operator delete[](void* memory, const char* file, int line);
```

### 内存跟踪宏

```cpp
#if ORG_TRACK_MEMORY && ORG_PLATFORM_WINDOWS
    #define onew new(__FILE__, __LINE__)  // 跟踪new
    #define odelete delete                // 跟踪delete
#else
    #define onew new                      // 标准new
    #define odelete delete                // 标准delete
#endif
```

### Tracy性能分析器集成

```cpp
#if ORG_ENABLE_PROFILING
    TracyAlloc(memory, size);   // 通知Tracy分配
    TracyFree(memory);          // 通知Tracy释放
#endif
```

### 线程安全机制

Memory系统采用互斥锁确保线程安全：

```cpp
// C++14兼容的线程锁
std::lock_guard<std::mutex> lock(s_Data->m_Mutex);

// 临界区保护
{
    std::lock_guard<std::mutex> lock(s_Data->m_Mutex);
    // 安全的内存映射操作
    Allocation& alloc = s_Data->m_AllocationMap[memory];
    alloc.Memory = memory;
    alloc.Size = size;
    alloc.Category = desc;
}
```

### 内存泄漏检测

系统提供内存泄漏检测功能：

```cpp
#ifndef ORG_DIST
    // 检查内存块是否在分配映射中
    if (!found) {
        ORG_CORE_WARN_TAG("Memory", "Memory block not present in alloc map");
    }
#endif
```

### 使用示例

#### 基础内存管理
```cpp
// 初始化内存系统
Orange::Allocator::Init();

// 基础分配
void* ptr = Orange::Allocator::Allocate(1024);

// 带描述分配
void* buffer = Orange::Allocator::Allocate(2048, "RenderBuffer");

// 释放内存
Orange::Allocator::Free(ptr);
Orange::Allocator::Free(buffer);
```

#### 使用跟踪宏
```cpp
// 带文件行号跟踪的分配
MyClass* obj = onew MyClass();

// 对应的释放
odelete obj;
```

#### 统计信息获取
```cpp
// 获取全局统计
const auto& globalStats = Orange::Memory::GetAllocationStats();
std::cout << "Total allocated: " << globalStats.TotalAllocated << " bytes" << std::endl;
std::cout << "Total freed: " << globalStats.TotalFreed << " bytes" << std::endl;

// 获取分类统计
const auto& statsMap = Orange::Allocator::GetAllocationStats();
for (const auto& pair : statsMap) {
    std::cout << "Category: " << pair.first 
              << ", Allocated: " << pair.second.TotalAllocated << " bytes" << std::endl;
}
```

### 错误修复记录

#### C++14兼容性修复
原代码使用了C++17的`std::scoped_lock`，修复为C++14兼容的`std::lock_guard`：

```cpp
// 修复前（C++17）
std::scoped_lock<std::mutex> lock(s_Data->m_Mutex);

// 修复后（C++14兼容）
std::lock_guard<std::mutex> lock(s_Data->m_Mutex);
```

#### Tracy集成修复
添加条件编译保护，避免Tracy头文件缺失导致的编译错误：

```cpp
#if ORG_ENABLE_PROFILING
    // Tracy只在启用性能分析时使用
    TracyAlloc(memory, size);
#endif
```

#### Logger集成修复
修正Logger宏调用格式，确保参数正确传递：

```cpp
// 修复Logger宏调用
ORG_CORE_WARN_TAG("Memory", "Memory block not present in alloc map");
```

### 性能特性

1. **零开销抽象**：在非跟踪模式下性能等同于标准malloc/free
2. **最小锁粒度**：只在关键数据结构操作时加锁
3. **内存对齐**：保持与系统分配器相同的内存对齐
4. **分类统计**：支持按文件或描述进行内存使用分析

### 平台支持

- **Windows**: 完整支持，包括全局操作符重载
- **其他平台**: 基础功能支持，不包含操作符重载

---

## Buffer系统技术文档

### 概述

Orange引擎的Buffer系统提供通用的内存缓冲区管理功能，支持多种数据类型的存储和操作。系统包含基础缓冲区类和自动内存管理的安全缓冲区类。

### 核心类设计

#### Buffer类

基础缓冲区类，提供内存缓冲区的分配、管理和访问功能：

```cpp
struct Buffer
{
    void* Data = nullptr;   // 缓冲区数据指针
    uint64_t Size = 0;      // 缓冲区大小（字节）
};
```

**构造方式**
```cpp
// 默认构造（空缓冲区）
Buffer buffer;

// 从指针和大小构造
Buffer buffer(data, size);

// 从std::array构造
std::array<float, 100> arr;
Buffer buffer(arr);

// 从std::vector构造
std::vector<int> vec;
Buffer buffer(vec);
```

**内存管理**
```cpp
// 分配内存
buffer.Allocate(1024);

// 释放内存
buffer.Release();

// 清零内存
buffer.ZeroInitialize();
```

**数据访问**
```cpp
// 类型化读取
float& value = buffer.Read<float>(offset);
const int& data = buffer.Read<int>(offset);

// 字节数组读取
byte* bytes = buffer.ReadBytes(size, offset);

// 数据写入
buffer.Write(data, size, offset);

// 数组访问
byte b = buffer[index];
buffer[index] = value;

// 类型转换
float* floatPtr = buffer.As<float>();
```

**实用功能**
```cpp
// 复制缓冲区
Buffer copy = Buffer::Copy(original);
Buffer copy2 = Buffer::Copy(data, size);

// 检查有效性
if (buffer) {
    // 缓冲区包含有效数据
}

// 获取大小
uint64_t size = buffer.GetSize();
```

#### BufferSafe类

继承自Buffer，提供RAII语义的自动内存管理：

```cpp
struct BufferSafe : public Buffer
{
    ~BufferSafe() { Release(); }  // 析构时自动释放内存
};
```

**使用示例**
```cpp
// 自动内存管理
{
    BufferSafe buffer;
    buffer.Allocate(1024);
    // 作用域结束时自动释放内存
}

// 复制创建
BufferSafe safeCopy = BufferSafe::Copy(data, size);
```

### 使用场景

#### 1. 文件数据处理
```cpp
// 读取文件到缓冲区
BufferSafe fileBuffer;
fileBuffer.Allocate(fileSize);
file.read(fileBuffer.As<char>(), fileSize);

// 解析文件头
FileHeader& header = fileBuffer.Read<FileHeader>();
```

#### 2. 网络数据包处理
```cpp
// 构造数据包
Buffer packet;
packet.Allocate(sizeof(PacketHeader) + dataSize);
packet.Write(&header, sizeof(PacketHeader));
packet.Write(data, dataSize, sizeof(PacketHeader));
```

#### 3. 渲染数据管理
```cpp
// 顶点数据缓冲区
std::vector<Vertex> vertices;
Buffer vertexBuffer(vertices);

// 上传到GPU
renderer.UploadVertexData(vertexBuffer.Data, vertexBuffer.Size);
```

#### 4. 序列化支持
```cpp
// 序列化对象
Buffer serializeBuffer;
serializeBuffer.Allocate(object.GetSerializedSize());
object.Serialize(serializeBuffer);

// 反序列化
Object newObject;
newObject.Deserialize(serializeBuffer);
```

### 设计特点

1. **简单高效**: 基于原始指针，避免不必要的抽象开销
2. **类型安全**: 模板化的读写接口，编译时类型检查
3. **内存管理**: 支持手动和自动两种内存管理方式
4. **容器兼容**: 直接支持STL容器的数据访问
5. **边界检查**: 在Debug模式下提供断言保护

### 注意事项

1. **内存安全**: Buffer类不自动管理内存，需要手动调用Release()
2. **边界检查**: 读写操作不进行运行时边界检查，需要调用者保证安全
3. **数据对齐**: 不处理数据对齐问题，需要调用者注意结构体对齐
4. **线程安全**: 不提供线程安全保证，多线程使用需要外部同步

---

## 智能指针和引用计数系统技术文档

### 概述

Orange引擎的智能指针系统提供线程安全的引用计数管理，完全兼容Hazel引擎API。系统包含RefCounted基类、Ref<T>强引用智能指针、WeakRef<T>弱引用智能指针和RefUtils生命周期跟踪工具。

### 核心组件

#### RefCounted基类

引用计数基类，所有需要智能指针管理的类都应继承此类：

```cpp
class RefCounted
{
public:
    virtual ~RefCounted() = default;
    
    void IncRefCount() const;    // 原子增加引用计数
    void DecRefCount() const;    // 原子减少引用计数
    uint32_t GetRefCount() const; // 获取当前引用计数
    
private:
    // 禁用拷贝操作以确保引用计数唯一性
    RefCounted(const RefCounted&) = delete;
    RefCounted& operator=(const RefCounted&) = delete;
    
    mutable std::atomic<uint32_t> m_RefCount = 0;
};
```

**关键设计决策**：
- 使用`std::atomic<uint32_t>`确保线程安全
- 禁用拷贝构造和赋值以避免引用计数混乱
- 虚析构函数支持多态删除

#### Ref<T>强引用智能指针

管理RefCounted派生对象的生命周期：

```cpp
template<typename T>
class Ref
{
public:
    // 构造和析构
    Ref();                          // 空指针构造
    Ref(T* instance);              // 原始指针构造
    Ref(const Ref& other);         // 拷贝构造
    Ref(Ref&& other);              // 移动构造
    ~Ref();                        // 自动释放引用
    
    // 工厂方法
    template<typename... Args>
    static Ref<T> Create(Args&&... args);
    
    // 类型转换
    template<typename T2>
    Ref<T2> As() const;
    
    // 访问操作
    T* operator->();               // 成员访问
    T& operator*();                // 解引用
    T* Raw();                      // 获取原始指针
    operator bool() const;         // 有效性检查
};
```

#### WeakRef<T>弱引用智能指针

不影响对象生命周期的观察者指针：

```cpp
template<typename T>
class WeakRef
{
public:
    WeakRef() = default;
    WeakRef(Ref<T> ref);
    WeakRef(T* instance);
    
    bool IsValid() const;          // 检查对象是否仍然存活
    operator bool() const;         // 等同于IsValid()
    
    T* operator->();
    T& operator*();
};
```

### 使用示例

#### 基本使用模式

```cpp
// 1. 定义继承RefCounted的类
class Texture : public Orange::RefCounted
{
public:
    Texture(const std::string& path);
    void Bind() const;
};

// 2. 使用Ref管理对象
Orange::Ref<Texture> texture = Orange::Ref<Texture>::Create("path/to/texture.png");

// 3. 传递和存储引用
class Material
{
    Orange::Ref<Texture> m_DiffuseTexture;
public:
    void SetDiffuseTexture(Orange::Ref<Texture> texture)
    {
        m_DiffuseTexture = texture; // 自动管理引用计数
    }
};
```

#### 避免循环引用

```cpp
class Parent : public Orange::RefCounted
{
    std::vector<Orange::Ref<Child>> m_Children; // 强引用子对象
};

class Child : public Orange::RefCounted
{
    Orange::WeakRef<Parent> m_Parent; // 弱引用父对象，避免循环引用
public:
    void DoSomething()
    {
        if (m_Parent.IsValid())
        {
            m_Parent->SomeMethod(); // 使用前检查有效性
        }
    }
};
```

---

## 基础组件系统技术文档

### 概述

Base.h提供Orange引擎的基础设施，包括平台检测、编译器适配、工具函数、智能指针别名和原子标志类。

### 平台检测

```cpp
// 自动平台检测
#if defined(_WIN32) || defined(_WIN64)
    #define ORG_PLATFORM_WINDOWS
#elif defined(__linux__)
    #define ORG_PLATFORM_LINUX
#elif defined(__APPLE__)
    #define ORG_PLATFORM_MACOS
#endif
```

支持的平台：
- **Windows**: Visual Studio 2019+, Clang
- **Linux**: GCC 9+, Clang 10+
- **macOS**: Clang (Xcode 12+)

### 编译器适配

```cpp
// 编译器检测
#if defined(__GNUC__)
    #if defined(__clang__)
        #define ORG_COMPILER_CLANG
    #else
        #define ORG_COMPILER_GCC
    #endif
#elif defined(_MSC_VER)
    #define ORG_COMPILER_MSVC
#endif

// 编译器特定宏
#ifdef ORG_COMPILER_MSVC
    #define ORG_FORCE_INLINE __forceinline
#elif defined(__GNUC__)
    #define ORG_FORCE_INLINE __attribute__((always_inline)) inline
#else
    #define ORG_FORCE_INLINE inline
#endif
```

### 原子标志系统

#### AtomicFlag - 线程安全标志

```cpp
struct AtomicFlag
{
    void SetDirty();                      // 设置脏状态
    bool CheckAndResetIfDirty();          // 检查并重置
    
    // 可拷贝但不拷贝状态
    AtomicFlag(const AtomicFlag&) noexcept {}
    AtomicFlag& operator=(const AtomicFlag&) noexcept { return *this; }
    
private:
    std::atomic_flag flag;
};
```

#### Flag - 高性能标志

```cpp
struct Flag
{
    void SetDirty() noexcept;             // 设置脏状态
    bool CheckAndResetIfDirty() noexcept; // 检查并重置
    bool IsDirty() const noexcept;        // 仅检查状态
    
private:
    bool flag = false;
};
```

**使用场景对比**：
- **AtomicFlag**: 多线程共享状态、渲染器状态同步
- **Flag**: 单线程逻辑、组件内部状态管理

### 数学工具函数

```cpp
template<typename T>
T RoundDown(T x, T fac) { return x / fac * fac; }

template<typename T>
T RoundUp(T x, T fac) { return RoundDown(x + fac - 1, fac); }
```

**应用场景**：
- **内存对齐**: `RoundUp(size, 16)` - 对齐到16字节边界
- **纹理尺寸**: `RoundUp(width, 4)` - 对齐到4像素边界
- **缓冲区大小**: 对齐到GPU要求的边界

---

## 断言系统技术文档

### 概述

Orange引擎的断言系统提供调试和验证机制，支持平台特定的调试断点和条件编译优化。

### 断言宏体系

```cpp
// 调试版本断言（仅在ORG_DEBUG模式下有效）
ORG_CORE_ASSERT(condition, "错误消息");
ORG_ASSERT(condition, "错误消息");

// 验证宏（在所有版本中有效）
ORG_CORE_VERIFY(condition, "错误消息");
ORG_VERIFY(condition, "错误消息");
```

**断言策略**：
- **ASSERT**: 调试时检查假设条件，发布版本中被移除
- **VERIFY**: 运行时检查关键条件，所有版本中都保留

### 平台调试断点

```cpp
#ifdef ORG_PLATFORM_WINDOWS
    #define ORG_DEBUG_BREAK __debugbreak()
#elif defined(ORG_COMPILER_CLANG)
    #define ORG_DEBUG_BREAK __builtin_debugtrap()
#else
    #define ORG_DEBUG_BREAK
#endif
```

### 使用最佳实践

```cpp
// 1. 参数验证
void SetViewportSize(uint32_t width, uint32_t height)
{
    ORG_CORE_ASSERT(width > 0 && height > 0, "视口尺寸必须大于0");
    ORG_CORE_ASSERT(width <= MAX_VIEWPORT_SIZE, "视口宽度超出限制");
}

// 2. 状态检查
void Renderer::BeginFrame()
{
    ORG_CORE_ASSERT(!m_FrameInProgress, "帧已经开始，不能重复调用BeginFrame");
    m_FrameInProgress = true;
}

// 3. 资源验证
void Texture::Bind(uint32_t slot)
{
    ORG_CORE_VERIFY(m_RendererID != 0, "纹理未初始化");
    ORG_ASSERT(slot < MAX_TEXTURE_SLOTS, "纹理槽位超出范围");
}
```

---

## Renderer模块技术文档

### 概述

Orange引擎的Renderer模块提供渲染器的基础抽象，包括类型定义、硬件能力查询和配置管理。

### RendererTypes - 类型定义

```cpp
/**
 * @brief 渲染器对象唯一标识符类型
 * 用于标识渲染器中各种对象（如纹理、缓冲区、着色器等）的唯一ID
 */
using RendererID = uint32_t;
```

**特性**：
- 使用32位无符号整数作为标识符
- ID值0保留作为无效ID或空ID使用
- 用于纹理、缓冲区、着色器等资源的唯一标识

### RendererCapabilities - 硬件能力查询

```cpp
struct RendererCapabilities
{
    std::string Vendor;        // 图形硬件厂商名称（如"NVIDIA Corporation"）
    std::string Device;        // 图形设备名称（如"GeForce RTX 4090"）
    std::string Version;       // 图形驱动程序版本
    int MaxSamples = 0;       // 支持的最大多重采样数
    float MaxAnisotropy = 0.0f; // 支持的最大各向异性过滤级别
    int MaxTextureUnits = 0;  // 支持的最大纹理单元数量
};
```

**用途**：
- 运行时查询和适配硬件能力
- 根据硬件信息调整渲染设置以获得最佳性能
- 提供硬件兼容性检查

### RendererConfig - 配置管理

```cpp
struct RendererConfig
{
    uint32_t FramesInFlight = 3;           // 飞行帧数
    bool ComputeEnvironmentMaps = true;     // 是否启用环境贴图计算
    uint32_t EnvironmentMapResolution = 1024;  // 环境贴图分辨率
    uint32_t IrradianceMapComputeSamples = 512; // 辐照度贴图计算采样数
    std::string ShaderPackPath;            // 着色器包文件路径
};
```

**配置说明**：
- **飞行帧数**: 同时在GPU上处理的帧数，影响延迟和性能
- **环境贴图**: 控制IBL（基于图像的光照）相关贴图的GPU计算
- **质量设置**: 环境贴图分辨率和采样数影响光照质量
- **着色器包**: 预编译着色器包路径，可减少启动时间

### 使用示例

```cpp
// 查询硬件能力
RendererCapabilities caps = renderer.GetCapabilities();
std::cout << "GPU: " << caps.Vendor << " " << caps.Device << std::endl;
std::cout << "Max MSAA: " << caps.MaxSamples << "x" << std::endl;

// 配置渲染器
RendererConfig config;
config.FramesInFlight = 2;  // 减少延迟
config.EnvironmentMapResolution = 512;  // 降低质量提高性能
if (caps.MaxSamples >= 4) {
    // 根据硬件能力调整设置
}

renderer.Initialize(config);
```

---

## Profiler系统技术文档

### 概述

Orange引擎的Profiler系统提供性能分析和调试支持，集成Tracy性能分析器，支持函数级别和作用域级别的性能跟踪。

### 系统配置

性能分析功能通过条件编译控制：

```cpp
#define ORG_ENABLE_PROFILING !ORG_DIST  // 非发布版本启用
```

### 核心宏定义

#### 帧标记
```cpp
// 标记帧边界（通常在主循环中调用）
ORG_PROFILE_MARK_FRAME
```

#### 函数和作用域分析
```cpp
// 函数级别分析（在函数开头使用）
ORG_PROFILE_FUNC()
ORG_PROFILE_FUNC("CustomName")

// 作用域分析
ORG_PROFILE_SCOPE()
ORG_PROFILE_SCOPE("ScopeName")

// 动态命名作用域
ORG_PROFILE_SCOPE_DYNAMIC(variableName)
```

#### 线程标记
```cpp
// 设置线程名称
ORG_PROFILE_THREAD("RenderThread")
ORG_PROFILE_THREAD("WorkerThread")
```

### 使用示例

#### 1. 基础函数分析
```cpp
void RenderFrame()
{
    ORG_PROFILE_FUNC();  // 分析整个函数
    
    // 渲染逻辑
    PrepareRenderData();
    ExecuteRenderCommands();
    PresentFrame();
}
```

#### 2. 作用域分析
```cpp
void UpdateGame(float deltaTime)
{
    ORG_PROFILE_FUNC();
    
    {
        ORG_PROFILE_SCOPE("Physics");
        physicsWorld.Step(deltaTime);
    }
    
    {
        ORG_PROFILE_SCOPE("AI");
        aiSystem.Update(deltaTime);
    }
    
    {
        ORG_PROFILE_SCOPE("Animation");
        animationSystem.Update(deltaTime);
    }
}
```

#### 3. 动态命名分析
```cpp
void ProcessAssets()
{
    for (const auto& asset : assets)
    {
        std::string scopeName = "Process_" + asset.GetName();
        ORG_PROFILE_SCOPE_DYNAMIC(scopeName.c_str());
        
        asset.Process();
    }
}
```

#### 4. 线程分析
```cpp
void RenderThreadMain()
{
    ORG_PROFILE_THREAD("RenderThread");
    
    while (running)
    {
        ORG_PROFILE_MARK_FRAME;
        
        {
            ORG_PROFILE_SCOPE("RenderLoop");
            ProcessRenderCommands();
        }
    }
}
```

#### 5. 主循环集成
```cpp
int main()
{
    ORG_PROFILE_THREAD("MainThread");
    
    while (gameRunning)
    {
        ORG_PROFILE_MARK_FRAME;
        
        {
            ORG_PROFILE_SCOPE("GameLoop");
            
            HandleInput();
            UpdateGame(deltaTime);
            RenderFrame();
        }
    }
    
    return 0;
}
```

### Tracy集成

当启用性能分析时，系统自动集成Tracy分析器：

```cpp
#if ORG_ENABLE_PROFILING 
    #include <tracy/Tracy.hpp>
    
    #define ORG_PROFILE_FUNC(...) ZoneScoped##__VA_OPT__(N(__VA_ARGS__))
    #define ORG_PROFILE_SCOPE_DYNAMIC(NAME) ZoneScoped; ZoneName(NAME, strlen(NAME))
#endif
```

### 性能影响

1. **Debug模式**: 完整的性能分析功能，有一定性能开销
2. **Release模式**: 保留性能分析，优化后开销较小
3. **Distribution模式**: 完全禁用，零性能开销

### 最佳实践

#### 1. 分析粒度
```cpp
// 好的做法：分析关键函数和热点代码
void CriticalFunction()
{
    ORG_PROFILE_FUNC();
    // 重要逻辑
}

// 避免：过度细粒度分析
void TrivialFunction()
{
    // ORG_PROFILE_FUNC();  // 不必要的开销
    return x + y;
}
```

#### 2. 命名规范
```cpp
// 使用有意义的名称
ORG_PROFILE_SCOPE("PhysicsSimulation");
ORG_PROFILE_SCOPE("TextureLoading");
ORG_PROFILE_SCOPE("ShaderCompilation");

// 避免无意义的名称
// ORG_PROFILE_SCOPE("Loop1");
// ORG_PROFILE_SCOPE("Temp");
```

#### 3. 线程标识
```cpp
// 为每个重要线程设置名称
void WorkerThread()
{
    ORG_PROFILE_THREAD("Worker");
    // 工作逻辑
}

void AudioThread()
{
    ORG_PROFILE_THREAD("Audio");
    // 音频处理
}
```

### 条件编译优化

在不同构建模式下，宏会自动优化：

```cpp
#if ORG_ENABLE_PROFILING
    // 启用完整分析功能
    #define ORG_PROFILE_FUNC(...) ZoneScoped##__VA_OPT__(N(__VA_ARGS__))
#else
    // 禁用时编译为空宏，零开销
    #define ORG_PROFILE_FUNC(...)
#endif
```

---

## Logger系统技术文档

### 概述

Orange引擎的Logger系统是一个高性能、功能丰富的日志记录框架，参考Hazel引擎设计理念，提供企业级的日志管理功能。系统支持多级别日志、标签化分类、条件编译优化等特性。

### Logger系统架构

#### 核心组件

**1. Logger类**
- 位置：`Src/OrangeEngine/Layer1/Core/Base/Logger.h/cpp`
- 功能：提供静态接口的日志管理器
- 特性：双日志器系统（Core/Client分离）

**2. 日志级别系统**
```cpp
enum class Level : uint8_t
{
    Trace = 0,    // 最详细的调试信息
    Debug,        // 调试过程中的详细信息
    Info,         // 一般性操作信息
    Warn,         // 潜在问题但不影响运行
    Error,        // 错误情况但程序可继续
    Critical      // 严重错误可能导致程序终止
};
```

**3. 日志器类型**
```cpp
enum class Type : uint8_t
{
    Core = 0,     // 引擎核心日志
    Client = 1    // 客户端应用日志
};
```

**4. 标签化系统**
```cpp
struct TagDetails
{
    bool mEnabled = true;                    // 标签是否启用
    Level mLevelFilter = Level::Trace;       // 最低日志级别过滤
};
```

### 预定义标签配置

| 标签名称 | 默认状态 | 默认级别 | 用途说明 |
|----------|----------|----------|----------|
| Core | 启用 | Trace | 核心系统 |
| Memory | 启用 | Error | 内存管理（仅显示错误） |
| Renderer | 启用 | Info | 渲染系统 |
| Audio | 启用 | Info | 音频系统 |
| Physics | 启用 | Warn | 物理系统 |
| Scripting | 启用 | Warn | 脚本系统 |
| Asset | 启用 | Warn | 资产管理 |
| Scene | 启用 | Info | 场景管理 |
| Animation | 启用 | Warn | 动画系统 |
| Input | 启用 | Info | 输入系统 |
| Network | 启用 | Warn | 网络系统 |
| Platform | 启用 | Warn | 平台相关 |
| Timer | 禁用 | Trace | 计时器（默认禁用避免过多输出） |
| ThirdParty | 启用 | Error | 第三方库 |

### API参考

#### 初始化和管理

```cpp
// 初始化日志系统（必须首先调用）
Orange::Logger::Init();

// 关闭日志系统（程序退出时调用）
Orange::Logger::Shutdown();

// 获取/修改标签配置
auto& tags = Orange::Logger::EnabledTags();
tags["MyTag"] = {true, Orange::Logger::Level::Debug};

// 重置为默认配置
Orange::Logger::SetDefaultTagSettings();
```

#### 基础日志记录

```cpp
// 核心日志宏
ORG_CORE_TRACE("详细跟踪信息");
ORG_CORE_DEBUG("调试信息");
ORG_CORE_INFO("一般信息");
ORG_CORE_WARN("警告信息");
ORG_CORE_ERROR("错误信息");
ORG_CORE_CRITICAL("严重错误");

// 客户端日志宏
ORG_TRACE("应用跟踪信息");
ORG_DEBUG("应用调试信息");
ORG_INFO("应用信息");
ORG_WARN("应用警告");
ORG_ERROR("应用错误");
ORG_CRITICAL("应用严重错误");
```

#### 标签化日志记录（推荐）

```cpp
// 核心标签日志
ORG_CORE_INFO_TAG("Renderer", "渲染器初始化完成");
ORG_CORE_WARN_TAG("Memory", "内存使用率达到80%");
ORG_CORE_ERROR_TAG("Physics", "物理引擎计算溢出");

// 客户端标签日志
ORG_INFO_TAG("Scene", "场景加载完成");
ORG_WARN_TAG("Asset", "纹理文件缺失");
ORG_ERROR_TAG("Network", "服务器连接失败");
```

#### 断言系统集成

```cpp
// 调试断言（仅Debug模式有效）
ORG_CORE_ASSERT(ptr != nullptr, "指针不能为空");
ORG_ASSERT(index < size, "索引越界");

// 验证检查（所有模式都有效）
ORG_CORE_VERIFY(result == SUCCESS, "操作失败");
ORG_VERIFY(file.is_open(), "文件打开失败");
```

### 条件编译优化

系统根据构建配置自动优化性能：

**Debug模式（ORG_DEBUG）**
- 启用所有日志级别
- 启用所有断言和验证
- 最详细的调试信息

**Release模式（ORG_RELEASE）**
- 禁用Trace和Debug级别日志
- 禁用断言，保留验证
- 平衡性能和信息量

**Distribution模式（ORG_DIST）**
- 只保留Critical级别日志
- 禁用断言和验证
- 最佳运行时性能

### 实现特点

#### 1. 简单可靠的实现
- 避免复杂的第三方依赖
- 使用基础的std::ofstream进行文件输出
- 保证在各种环境下的编译稳定性

#### 2. 线程安全
- 内部使用互斥锁保护共享数据
- 支持多线程环境下的并发日志记录
- 与内存管理系统协同工作

#### 3. 性能优化
- 宏展开避免函数调用开销
- 条件编译消除不需要的代码
- 最小化字符串处理开销

#### 4. 平台集成
- Windows平台支持MessageBox断言显示
- 为tracy性能分析器预留接口
- 支持跨平台编译

### 使用最佳实践

#### 1. 标签选择
```cpp
// 好的做法：使用有意义的标签
ORG_CORE_INFO_TAG("Renderer", "Shader compiled successfully");
ORG_INFO_TAG("GameLogic", "Player level up to " + std::to_string(level));

// 避免：过于频繁的日志
// ORG_CORE_TRACE_TAG("Timer", "Frame time: " + deltaTime); // 每帧都输出
```

#### 2. 级别选择
```cpp
// Critical: 导致程序无法继续的严重错误
ORG_CORE_CRITICAL_TAG("Core", "Failed to initialize graphics device");

// Error: 功能性错误但程序可以继续
ORG_CORE_ERROR_TAG("Asset", "Failed to load texture: " + filename);

// Warn: 潜在问题或不推荐的使用方式
ORG_CORE_WARN_TAG("Memory", "Memory usage exceeds 80%");

// Info: 重要的状态变化和操作结果
ORG_CORE_INFO_TAG("Scene", "Scene loaded: " + sceneName);

// Debug: 调试时需要的详细信息
ORG_CORE_DEBUG_TAG("Physics", "Collision detected between objects");

// Trace: 最详细的执行流程跟踪
ORG_CORE_TRACE_TAG("Core", "Entering main loop iteration");
```

#### 3. 初始化序列
```cpp
int main()
{
    // 1. 首先初始化日志系统
    Orange::Logger::Init();
    
    // 2. 配置自定义标签（可选）
    auto& tags = Orange::Logger::EnabledTags();
    tags["GameLogic"] = {true, Orange::Logger::Level::Info};
    
    // 3. 记录启动信息
    ORG_CORE_INFO_TAG("Core", "Orange Engine starting up");
    
    // ... 应用程序逻辑 ...
    
    // 4. 程序退出前关闭日志系统
    ORG_CORE_INFO_TAG("Core", "Orange Engine shutting down");
    Orange::Logger::Shutdown();
    
    return 0;
}
```

### 与内存系统集成

Logger系统与Orange内存管理系统深度集成，支持内存分配的日志记录：

```cpp
// 内存分配失败时的自动日志记录
void* ptr = Orange::Allocator::Allocate(1024, "RenderBuffer");
if (!ptr) {
    ORG_CORE_ERROR_TAG("Memory", "Failed to allocate render buffer");
}

// 内存泄漏检测的日志输出
ORG_CORE_WARN_TAG("Memory", "Memory block not present in alloc map");
```

### 文件输出

日志文件自动创建在`logs/`目录下：
- `ORANGE_CORE.log`: 核心系统日志
- `ORANGE_CLIENT.log`: 客户端应用日志

文件格式：`[TYPE] LEVEL: MESSAGE`
例如：`[CORE] INFO: Engine initialized successfully`

### 扩展指南

#### 添加新标签
```cpp
// 在Logger::s_DefaultTagDetails中添加新条目
{ "MySystem", TagDetails{ true, Level::Info } },

// 在代码中使用
ORG_CORE_INFO_TAG("MySystem", "My system initialized");
```

#### 自定义输出格式
可以修改`SimpleLoggerImpl::Log`方法来改变输出格式，例如添加时间戳、线程ID等信息。

### 故障排除

#### 常见问题

1. **日志不显示**
   - 检查Logger::Init()是否被调用
   - 检查标签是否启用
   - 检查日志级别是否足够

2. **编译错误**
   - 确保包含正确的头文件
   - 检查ORG_DEBUG等宏定义
   - 验证CMake配置

3. **性能问题**
   - 在Release/Distribution模式下检查
   - 减少高频日志输出
   - 考虑提高日志级别过滤
